import styled from 'styled-components';
import { colors } from '../../../configs';

const Wrapper = styled.div`
  background-color: ${colors.white};
  margin: auto;
  padding: 20px 28px;
  box-shadow: 0px 4px 22px rgb(0 0 0 / 8%);
  border-radius: 10px;
  margin-top: -80px;
  .component_filter_form {
  }
`;

export default Wrapper;
